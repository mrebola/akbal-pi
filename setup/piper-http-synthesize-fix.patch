--- a/src/cloud-api/local/piper-http-tts.ts
+++ b/src/cloud-api/local/piper-http-tts.ts
@@ -66,7 +66,7 @@
       `{ "text": "${escapedText}", "length_scale": ${piperHttpLengthScale} }`,
       "-o",
       tempWavFile,
-      `${piperHttpHost}:${piperHttpPort}`
+      `${piperHttpHost}:${piperHttpPort}/synthesize`
     ]);

     piperProcess.stdin.write(text);
